# Membership methods

[`methods.py`](methods.py) provides one import point for creating, retrieving,
and listing programs and tiers; member enrolment, lookup and updates;
check-in/out; setting, earning and burning points; changing tiers; member event
history; and deletion. The individual scripts remain available for backwards
compatibility.

Run the complete lifecycle with `python main.py membership`.
