# Coupon methods

[`methods.py`](methods.py) provides one import point for creating, copying,
retrieving, updating and listing campaigns and offers; coupon lookup by ID or
external ID; issuing, counting, redeeming and voiding; and deletion. The
individual scripts remain available for backwards compatibility.

Run the complete lifecycle with `python main.py coupons`.
