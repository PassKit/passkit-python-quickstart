# Flight methods

[`methods.py`](methods.py) provides one import point for creating, retrieving,
updating and deleting carriers, airports, flights and flight designators, plus
boarding-pass creation, record lookup, download and update. The individual
scripts remain available for backwards compatibility.

Run the complete lifecycle with `python main.py flights`.
